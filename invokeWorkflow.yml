---
  - hosts: localhost

  - tasks:
      - name: Launch a workflow with a timeout of 10 seconds
        workflow_launch:
            workflow_template: "AWS_Akshay_Workflow"
            timeout: 10

