--- # Install tools
 - hosts: ec2-13-232-41-14.ap-south-1.compute.amazonaws.com
   become: yes

   vars_prompt:

      - name: tools
        prompt: Enter Tools (comma seperated)
        private: no

   tasks:
      - set_fact:
          application_list: "{{ tools.split(',') }}"

      - name: get tools
        yum:
          name: "{{item}}"
          state: present
        loop: "{{application_list}}"
        when: application_list is iterable

